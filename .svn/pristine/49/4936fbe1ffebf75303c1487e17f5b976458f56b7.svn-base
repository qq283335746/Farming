define(['app'],function (app) {
    app.controller('indexCtrl', ['$scope', 'Storage', function($scope, Storage) {
        $scope.enterView = function(){
            Storage.set("DM_First", "true");
            location.href = "#/login";
        }
    }]);
});
    
